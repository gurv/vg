server:
  port: 8082

spring:
  application:
    name: metadata
  cloud:
    config:
      fail-fast: true
      discovery:
        enabled: true
        serviceId: config

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/