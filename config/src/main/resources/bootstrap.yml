server:
  port: 8888

spring:
  application:
    name: config
  cloud:
    consul:
      host: consul
    config:
      server:
        native:
          searchLocations: classpath:/shared
  profiles:
     active: native
