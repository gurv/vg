task dockerImage() {
    description = "Build docker-images project '$project.path'."
    group = rootProject.dockerTaskGroup
    dependsOn = [
            'kafka:dockerImage',
            'zookeeper:dockerImage',
    ]
}
