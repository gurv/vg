server:
  port: 10000

sidecar:
  port: 8000

spring:
  application:
    name: gateway

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

encrypt:
  failOnError: false

endpoints:
  restart:
    enabled: true